import React from 'react';

// Full two-pane notes editor lands in milestone 5.5.
export function NotesView({ initialNoteId: _n }: { initialNoteId?: string }): React.JSX.Element {
  return <div style={{ padding: 'var(--sp-6)', color: 'var(--text-3)' }}>Notes</div>;
}
